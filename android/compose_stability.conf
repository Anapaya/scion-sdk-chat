// The generated API models are immutable data classes, but :chat-client is compiled without the
// Compose compiler, so they carry no stability metadata and are inferred unstable. Without this,
// strong skipping compares them by identity and every row recomposes on every poll.
com.anapaya.chat.client.model.*
